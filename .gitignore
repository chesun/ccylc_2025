# ─────────────────────────────────────────────────────────────
# DATA — restricted. Lives on Scribe only, NEVER in this repo.
# (CCYLC survey responses include respondent emails / minors' data.)
# ─────────────────────────────────────────────────────────────
dta/
data/
*.csv
*.dta

# Data-bearing exports (can contain raw values)
out/
output/

# Run logs ARE tracked (a version history of what ran) — keep them PII-free.

# OS / editor cruft
.DS_Store
*~
